import { Router } from 'express';
import { searchAndUpdateProductByName } from '../service/finders';

const router = Router();

//router.put('/update-by-name/:name', searchAndUpdateProductByName); 

export default router;