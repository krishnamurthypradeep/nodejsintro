import Shape from "./shape";

export default class Rectangle extends Shape{
    constructor(width: number,height: number){
        super(width,height)
    }
    area(): number {
        return this.width * this.height
    }
    toString(){
        return "Rectangle"
    }
}
// java -> mvn | gradle (mvn repository) pom.xml
// nodejs -> npm (node package manager) (npm registry) package.json

// springboot  
// node.js (node)
// serverless programming

// TypeScript

// Form (TS )
// form.ts (api request)
// clickevent.ts
// TS -> JS -> Bundle it To Single JS

// Spring Boot (Microservices)(Day 4 & 5) 
// Node.JS
// Python



// TypeScript Server Side Code
// main.ts
// api.ts (http://IP:PORT)
// Node.JS Server 